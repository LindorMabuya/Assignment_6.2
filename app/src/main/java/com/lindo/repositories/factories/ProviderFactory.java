package com.lindo.repositories.factories;


import com.lindo.repositories.domain.DemographicInfo;
import com.lindo.repositories.domain.Payer;
import com.lindo.repositories.domain.Provider;

/**
 * Created by bishop v on 2016-10-31.
 */
public class ProviderFactory {/*
    public static Provider getProvider(long id, DemographicInfo demographicInfo, Payer payer){
        return  new Provider.Builder()
                .demographicInfo(id)
                .demographicInfo(demographicInfo)
                .payer(payer)
                .build();
    }*/
}
